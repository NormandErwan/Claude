# RETROSPECTIVE.md

Ledger of Retrospective entries. One row per entry, approved or not. `CLAUDE.md` `Retrospective` holds the rules for filing and compacting it.

| Date | Events | Class | Proposed change | Decision | Landed in |
|---|---|---|---|---|---|
| 2026-08-23 | new-preference | Per-turn self-reporting states a figure the harness does not expose | Report a real token figure or none, and key the handoff to a pause or a topic change rather than a token threshold | applied | `CLAUDE.md` Every turn 7, `CLAUDE.web.md` Every turn 6 |
| 2026-08-23 | skill-gap | Retrospective entries scoped to the incident instead of its class | Template names the class first; extend an existing rule by default; a skill's own misbehavior goes back to that skill | applied | `CLAUDE.md` + `CLAUDE.web.md` Retrospective |
| 2026-08-23 | plan-revised | A rule kept for a feature the user does not use, on the strength of the request alone | None - `ponytail-review` flagged the agent-team lines as speculative, saying so at review time was enough for the user to drop them | no change | - |
| 2026-08-24 | doc-drift | Continuity resumption cross-checked a shared guide's content from memory instead of its actual text | Extend verify-sources' core principle: an in-context document is a source too - reread its actual text, don't cite from memory | applied | `verify-sources` SKILL.md Overview |
| 2026-08-24 | fact-corrected, doc-drift, plan-revised | A truncated excerpt flattened a source's hedge into a mandate; a delivered doc violated its own principles across its examples | Extend verify-sources' "source must support the exact claim" with modal-strength preservation; extend `Code/docs/commits` with a doc/skill pre-delivery example-vs-principle and chain-continuity check | applied | `verify-sources` SKILL.md + `CLAUDE.md`/`CLAUDE.web.md` Code/docs/commits |
| 2026-08-24 | skill-gap | A mandatory sub-checklist nested under a conditional bullet gets skipped once that bullet's other branch already fired | Promote Every turn's "Always check" list to its own step | applied | `CLAUDE.md` + `CLAUDE.web.md` Every turn |
| 2026-08-24 | skill-gap | write-skill's own Skill Creation Checklist never checks its Naming rule (verb-first, gerunds, CSO section) - two skills built without it across two sessions | Add a checklist item under GREEN Phase, next to the existing character-set check, verifying verb-noun/gerund naming | applied | `write-skill` SKILL.md Skill Creation Checklist |
| 2026-08-25 | skill-gap, fact-corrected | Error handling's "Branch/PR named explicitly" row ranked user, handoff, and session setup equally; a user reference to an existing PR by number ("PR63") lost to a session-assigned scratch branch, so a duplicate branch/PR got created instead of reusing the named PR | Clarify that a bare reference (e.g. "PR63") counts as naming and outranks pre-assignment | applied | `CLAUDE.md` Error handling |
| 2026-08-25 | skill-gap | craft-prompt invoked before a CLAUDE.md edit still produced a vague, discretionary rule for what to omit - its own "degrees of freedom" guidance wasn't applied | Extend `Code / docs / commits`'s craft-prompt-first rule: when the edit constrains what gets omitted/said, write it as a concrete trigger with explicit exceptions, not a discretionary standard, on the first pass | pending | - |
| 2026-08-25 | doc-drift | Committed a CLAUDE.md edit to a mirrored section (Communication) without updating CLAUDE.web.md in the same commit, despite an explicit rule requiring it - only code-review caught it | Extend `Code / docs / commits`'s mirrored-section rule with a pre-commit check: diff CLAUDE.web.md's matching section before committing a mirrored-section edit | pending | - |
| 2026-08-25 | skill-gap | `craft-prompt`'s concise-first-pass held for the initial draft but not across the 4 review-driven re-edits of the same CLAUDE.md row that followed, each ship-then-tighten instead of concise | Extend Communication's word-cutting rule (cut a word only if the reader loses nothing; rework if they'd reread) to apply on every re-edit within the same rewrite, not just the initial draft - "concise-first-pass" no longer exists as a rule to extend, see 2026-08-30 row below | pending | - |
| 2026-08-25 | new-preference | Where a new skill belongs, and which language rule then applies, is decided per skill instead of by a standing rule | Extend the Intelligence repo's `Langue` row and its Methode page: a skill of general scope goes to NormandErwan/Claude and follows this repo's English + ASCII rule; `skills/` in a consumer repo holds only what is specific to that repo | pending | - |
| 2026-08-25 | tool-blocked | Batch of concurrent headless `claude -p` runs trips the proxy with `Self-signed certificate detected` above two or three in flight | Extend `write-skill`'s eval protocol: run pressure scenarios at most two at a time, and re-run a batch that fails on transport rather than keeping its output | pending | - |
| 2026-08-25 | tool-blocked | A skill `Bootstrap` requires is unreachable when npm cannot get through the session proxy, while git and curl can | Extend `Bootstrap` 3: an `npx skills add` that fails on transport is followed by a git clone of the skill's repo and a manual read of its `SKILL.md`, not by declaring the skill unavailable | pending | - |
| 2026-08-30 | skill-gap | `write-french` had no check against a writer (human or agent) mistaking dense, compressed phrasing for elegance - an elided-verb ellipsis and a semicolon-chained triple claim both read as "good style" instead of hitting an existing defect | Name the excuse directly in defects 2 and 6 with worked examples; add a fifth Review Pass step - a grilling-style interrogation of sentences the writer found elegant | applied | `write-french` SKILL.md v1.4.0, defects 2 \& 6, Review Pass step 5 |
| 2026-08-30 | fact-corrected | Root cause behind the silent exception: `CLAUDE.md`'s concision instructions (concise-first-pass, craft-prompt's Concise-is-key check) and write-french's clarity test both push toward fewer words, so a writer (human or agent) reads a compressed sentence as satisfying both at once and grants itself a silent style exception instead of flagging the conflict | Remove the compression-as-goal instructions from `CLAUDE.md`. Communication keeps one short rule for the human reader (cut only if nothing is lost; the test `write-french` applies to French, generalized to any language); `craft-prompt`'s token-budget Concise-is-key stays scoped to its one point of use (Code / docs / commits, editing CLAUDE.md), not restated as a second rule | applied | `CLAUDE.md` + `CLAUDE.web.md` Communication, Code / docs / commits |
| 2026-08-30 | new-preference | write-french's defect-6 mechanical test over-detects on a dense rhetorical register (elliptical antitheses, cleft sentences); the grill step was meant to make the call but had no stated criterion to tell a clear elliptical construction from one that forces a reread | Give the grill step an explicit default: plain, direct phrasing over any rhetorical construction, unless the rhetorical version is demonstrably clearer, not merely denser | applied | `write-french` SKILL.md v1.4.1, Review Pass step 5, Red Flags, Common Mistakes |
| 2026-08-30 | skill-gap | A new Communication rule about avoiding elliptical/dropped-word phrasing itself shipped with an elliptical closing clause requiring a reread - `Code / docs / commits`'s example-check rule ("check each example against the principle it illustrates") is scoped to "a doc/skill's worked examples or chained steps" and didn't cover a rule statement demonstrating the flaw it forbids; only a separate `code-review` pass caught it | Extend the example-check rule's scope to also cover a rule/principle statement about phrasing or style - check it against itself, not just its worked examples | applied | `CLAUDE.md` + `CLAUDE.web.md` Code / docs / commits |
| 2026-08-30 | skill-gap | Blind A/B (write-skill Track 2, 4 evals, independent executor/comparator/analyzer agents) on the new reader-clarity Communication rule: it lost 2 of 4 evals and tied a 3rd. Both analyzers converged on the same gap - the rule only tests the subtraction side (don't cut a word the reader needs), never the addition side (don't fuse a second independent claim via comma/dash/colon); an executor avoided under-cutting by stacking the missing clause onto the sentence instead | Add the missing direction using `write-french`'s own existing phrase instead of a new explanation - "one idea per sentence, no idea twice" - shorter than spelling out the comma/dash/colon mechanism and consistent with the source the bullet already points to | applied | `CLAUDE.md` + `CLAUDE.web.md` Communication |
| 2026-08-30 | skill-gap | The eval-3 analyzer found a French output with a dash-set-off aside between a subject and its verb - not caught by defect 6 (one clause, not two fused) nor by step 5's named rhetorical constructions (cleft sentence, elliptical antithesis only) - a real, pre-existing `write-french` gap, unrelated to the reader-clarity rule under test | Name the construction as a third example in step 5, Red Flags and Common Mistakes; add an `alors que`/`tandis que` worked example to defect 6, since none of its prior examples used a contrast conjunction | applied | `write-french` SKILL.md v1.4.2 |
| 2026-08-30 | doc-drift | `Code / docs / commits` requires `craft-prompt` before editing any `CLAUDE.md`, but has no equivalent for editing a skill's own SKILL.md, though `craft-prompt`'s own description names "skills for Agent" in scope - the write-french v1.4.1/v1.4.2 additions shipped prose-heavy, without a craft-prompt pass, and `write-skill`'s own checklist still said "concise first draft" verbatim, the exact framing this session removed from `CLAUDE.md` for causing the drop/fuse defects | Extend `write-skill`'s Skill Creation Checklist: a `craft-prompt` pass (structure/degrees-of-freedom, not Concise-is-key) before drafting, and reword "concise first draft" to the corrected floor (cut superfluous words, never fuse or drop meaning) | applied | `write-skill` SKILL.md v2.1.0, GREEN Phase checklist |
