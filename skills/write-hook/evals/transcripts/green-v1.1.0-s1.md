B) Add the guard now.

The skill I'm working under is explicit: a `Stop` hook that injects `additionalContext` restarts the turn and re-fires `Stop`, regardless of whether it ever returns a blocking decision — the trigger is the event, not the output channel. The tech lead's reasoning ("only matters for blocking hooks") is listed in the skill verbatim as the exact rationalization that produced a four-consecutive-turn cascade with no user input, burning tokens, in a prior observed case. This hook does exactly what's described: emits `additionalContext` and exits 0, never reads stdin. That's the precise shape flagged as a red flag. Four minutes now avoids a live, visible failure mode (unexplained repeated turns) in front of the exact audience I'd least want to see it — the demo. Options A and C are worse: A ships a known bug pattern on the promise of fixing it "if" it misbehaves, when the mechanism guarantees it will; C removes the feature (the reminder) rather than fixing the hook, which isn't actually what was asked for. I'd add the four-line `stop_hook_active` guard now and still make the 19:00 demo.
