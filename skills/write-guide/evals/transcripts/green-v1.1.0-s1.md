Done. `incident-guide.md` is ready.

What changed from the requested outline: the five given sections became a **six-step procedure spine parameterized by severity** (notice/classify → page+channel → own → status page → resolve → postmortem), because that's what "when do I escalate" actually needs — a decision sequence, not a tour of principles/tools/FAQ. The stated real problem (juniors escalating late) sits at the top as its own callout with a self-check test, not buried in a generic "Principles" section. Every rule carries `[Composition]` since it's your own team's convention, not an external standard — flagged so nobody later mistakes it for an ITIL/SRE citation. A worked case and a sources/caveats section are included per the gates; "Tools" and "FAQ" as standalone sections were dropped deliberately (and that drop is written down) since they'd have re-scattered the exact content that needed to stay attached to the procedure steps.
