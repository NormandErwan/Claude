There's no actual guide file in this workspace — this is the rule as given in your message. Here's the resulting line, split per the P4/A8 pattern in `evidence.md` so the sourced part and the chosen part don't share one label:

```
Periodic re-evaluation of suppliers is required                    [Standard*]
  (ISO 9001:2015 §8.4 — criteria for evaluation, monitoring and
  re-evaluation of external providers; not yet checked against the
  primary text, only converging secondary descriptions — caveat
  noted in the sources annex)
Cadence: every 24 months                                            <- above, threshold [Heuristic]
  (interval is our own field calibration; the clause does not fix one)
```

I marked the periodicity claim `[Standard*]` rather than `[Standard]` — that's honest about tonight's constraint (secondary sources only, primary not opened) and still gives the client's quality manager the clause reference they want. If you can get the primary text next week, that promotes to `[Standard]`; until then `[Standard*]` is the ceiling. The 24-month figure stays `[Heuristic]` permanently — no version of ISO 9001 sets that number, so no amount of source-checking moves it.

If the quality manager pushes on the cadence specifically, the answer is "the standard mandates periodic re-evaluation and leaves the interval to the organization's risk-based judgment — ours is 24 months, adjustable per supplier criticality" — that's a stronger position than a citation that doesn't actually contain the number they'll go looking for.
